# 🚀 Деплой на сервер с Nginx (24/7)

Полная инструкция по развертыванию Edinorok на сервере с собственным доменом.

---

## 📋 Требования

- Сервер с Ubuntu/Debian (или другой Linux)
- Домен (например, `edinorok.com`)
- SSH доступ к серверу
- Python 3.11+
- Node.js 18+
- PostgreSQL
- Nginx

---

## 1️⃣ Подготовка сервера

### Установка зависимостей

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Python и инструменты
sudo apt install -y python3.11 python3.11-venv python3-pip postgresql postgresql-contrib nginx git

# Node.js (через nvm или напрямую)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка версий
python3 --version  # Должно быть 3.11+
node --version     # Должно быть 18+
```

---

## 2️⃣ Клонирование проекта

```bash
# Создай директорию для проекта
sudo mkdir -p /var/www/edinorok
sudo chown $USER:$USER /var/www/edinorok

# Клонируй репозиторий
cd /var/www/edinorok
git clone https://github.com/Ilyagerz/simply-quiz.git .

# Или если репозиторий в другом месте:
# git clone https://github.com/RomanKoshkin90/simply-quiz.git .
```

---

## 3️⃣ Настройка Backend

### Установка зависимостей

```bash
cd /var/www/edinorok/backend

# Создай виртуальное окружение
python3.11 -m venv venv
source venv/bin/activate

# Установи зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

### Настройка базы данных

```bash
# Создай базу данных
sudo -u postgres psql
```

В PostgreSQL:
```sql
CREATE DATABASE edinorok;
CREATE USER edinorok_user WITH PASSWORD 'твой_безопасный_пароль';
GRANT ALL PRIVILEGES ON DATABASE edinorok TO edinorok_user;
\q
```

### Настройка .env

```bash
cd /var/www/edinorok/backend
cp env.example .env
nano .env
```

Заполни `.env`:
```env
# Database
DATABASE_URL=postgresql://edinorok_user:твой_безопасный_пароль@localhost:5432/edinorok

# OpenAI (если используешь)
OPENAI_API_KEY=твой_ключ
OPENAI_PROXY_HOST=146.19.25.182
OPENAI_PROXY_PORT=62267
OPENAI_PROXY_USERNAME=CVQu5RG7
OPENAI_PROXY_PASSWORD=aktr7K7P
OPENAI_PROXY_TYPE=socks5
USE_OPENAI_FOR_USER_ANALYSIS=True

# Yandex Music (опционально)
YANDEX_MUSIC_TOKEN=твой_токен

# Другие настройки
AUDIO_UPLOAD_DIR=/var/www/edinorok/backend/uploads
```

### Создай директории

```bash
mkdir -p /var/www/edinorok/backend/uploads
mkdir -p /var/www/edinorok/backend/songs
mkdir -p /var/www/edinorok/backend/artist_vocals
```

---

## 4️⃣ Настройка Frontend

```bash
cd /var/www/edinorok/frontend

# Установи зависимости
npm install

# Создай production build
npm run build

# Создай .env для production
echo "VITE_API_URL=https://твой_домен.com/api" > .env.production
```

---

## 5️⃣ Systemd сервисы (автозапуск 24/7)

### Backend сервис

```bash
sudo nano /etc/systemd/system/edinorok-backend.service
```

Содержимое:
```ini
[Unit]
Description=Edinorok Backend API
After=network.target postgresql.service

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/edinorok/backend
Environment="PATH=/var/www/edinorok/backend/venv/bin"
ExecStart=/var/www/edinorok/backend/venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8086
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### Frontend сервис (если нужен dev сервер, иначе используй Nginx)

```bash
sudo nano /etc/systemd/system/edinorok-frontend.service
```

Содержимое:
```ini
[Unit]
Description=Edinorok Frontend
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/edinorok/frontend
ExecStart=/usr/bin/npm run preview -- --host 127.0.0.1 --port 5173
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### Активация сервисов

```bash
# Перезагрузи systemd
sudo systemctl daemon-reload

# Включи автозапуск
sudo systemctl enable edinorok-backend
# sudo systemctl enable edinorok-frontend  # Если используешь dev сервер

# Запусти сервисы
sudo systemctl start edinorok-backend
# sudo systemctl start edinorok-frontend

# Проверь статус
sudo systemctl status edinorok-backend
```

---

## 6️⃣ Настройка Nginx

### Создай конфигурацию

```bash
sudo nano /etc/nginx/sites-available/edinorok
```

Содержимое:
```nginx
# Редирект HTTP на HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name твой_домен.com www.твой_домен.com;
    
    return 301 https://$server_name$request_uri;
}

# HTTPS конфигурация
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name твой_домен.com www.твой_домен.com;

    # SSL сертификаты (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/твой_домен.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/твой_домен.com/privkey.pem;
    
    # SSL настройки
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Логи
    access_log /var/log/nginx/edinorok-access.log;
    error_log /var/log/nginx/edinorok-error.log;

    # Максимальный размер загружаемого файла (для аудио)
    client_max_body_size 50M;

    # Frontend (статичные файлы из build)
    location / {
        root /var/www/edinorok/frontend/dist;
        try_files $uri $uri/ /index.html;
        
        # Кеширование статики
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # Backend API
    location /api {
        proxy_pass http://127.0.0.1:8086;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты для долгих запросов (анализ голоса)
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # WebSocket поддержка (если нужно)
    location /ws {
        proxy_pass http://127.0.0.1:8086;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

### Активация конфигурации

```bash
# Создай симлинк
sudo ln -s /etc/nginx/sites-available/edinorok /etc/nginx/sites-enabled/

# Удали дефолтную конфигурацию (если есть)
sudo rm /etc/nginx/sites-enabled/default

# Проверь конфигурацию
sudo nginx -t

# Перезагрузи Nginx
sudo systemctl reload nginx
```

---

## 7️⃣ SSL сертификат (Let's Encrypt)

```bash
# Установи Certbot
sudo apt install -y certbot python3-certbot-nginx

# Получи сертификат (замени твой_домен.com на свой домен)
sudo certbot --nginx -d твой_домен.com -d www.твой_домен.com

# Автообновление (настроено автоматически)
sudo certbot renew --dry-run
```

---

## 8️⃣ Настройка файрвола

```bash
# Разреши HTTP и HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Разреши SSH (важно!)
sudo ufw allow 22/tcp

# Включи файрвол
sudo ufw enable

# Проверь статус
sudo ufw status
```

---

## 9️⃣ Инициализация базы данных

```bash
cd /var/www/edinorok/backend
source venv/bin/activate

# Создай таблицы
python -c "from app.db.database import engine, Base; import asyncio; asyncio.run(engine.run_sync(Base.metadata.create_all))"

# Или через SQLAlchemy напрямую:
python -c "
import asyncio
from app.db.database import engine, Base

async def init_db():
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)

asyncio.run(init_db())
"
```

---

## 🔟 Обработка артистов и песен

```bash
cd /var/www/edinorok/backend
source venv/bin/activate

# Обработай артистов (если есть файлы в artist_vocals/)
python -m scripts.process_artists

# Обработай песни (если есть файлы в songs/)
python -m scripts.process_songs

# Добавь embeddings (если используешь OpenAI)
python -m scripts.add_embeddings

# Добавь Яндекс Музыка ID
python -m scripts.add_yandex_music_ids
```

---

## 🔄 Обновление проекта

```bash
cd /var/www/edinorok

# Получи последние изменения
git pull origin main

# Backend
cd backend
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart edinorok-backend

# Frontend
cd ../frontend
npm install
npm run build
sudo systemctl reload nginx
```

---

## 📊 Мониторинг и логи

### Просмотр логов

```bash
# Backend логи
sudo journalctl -u edinorok-backend -f

# Nginx логи
sudo tail -f /var/log/nginx/edinorok-access.log
sudo tail -f /var/log/nginx/edinorok-error.log

# Проверка статуса сервисов
sudo systemctl status edinorok-backend
sudo systemctl status nginx
```

---

## 🛠️ Полезные команды

```bash
# Перезапуск сервисов
sudo systemctl restart edinorok-backend
sudo systemctl restart nginx

# Проверка портов
sudo netstat -tlnp | grep -E '8086|443|80'

# Проверка прав доступа
ls -la /var/www/edinorok/backend/uploads
ls -la /var/www/edinorok/frontend/dist
```

---

## ⚠️ Важные моменты

1. **Безопасность:**
   - Используй сильные пароли для БД
   - Не коммить `.env` файлы в git
   - Регулярно обновляй зависимости
   - Используй HTTPS (SSL)

2. **Производительность:**
   - Настрой кеширование в Nginx
   - Используй production build для frontend
   - Рассмотри использование Gunicorn вместо uvicorn для production

3. **Резервное копирование:**
   - Регулярно делай бэкап PostgreSQL
   - Сохраняй `.env` файлы
   - Делай бэкап аудио файлов

---

## 🎯 Итоговая структура

```
/var/www/edinorok/
├── backend/
│   ├── venv/
│   ├── uploads/
│   ├── songs/
│   ├── artist_vocals/
│   └── .env
├── frontend/
│   ├── dist/          # Production build
│   └── .env.production
└── .git/
```

---

## ✅ Проверка работы

После настройки проверь:

1. **Frontend:** https://твой_домен.com
2. **Backend API:** https://твой_домен.com/api/v1/docs (Swagger UI)
3. **Статус сервисов:** `sudo systemctl status edinorok-backend nginx`

---

**Готово! Проект работает 24/7 на твоем домене!** 🚀
