# 📤 Добавление аудио файлов и БД в GitHub

`.gitignore` обновлен - теперь аудио файлы и БД **НЕ исключаются** из git.

---

## ⚠️ ВАЖНО: Размер файлов

**Обнаружено:**
- `backend/artist_vocals/`: **583 MB** (126 файлов)
- `backend/songs/`: **1.1 GB** (много файлов)
- `backend/uploads/`: **54 MB**
- **Итого: ~1.7 GB** 🚨

GitHub имеет ограничения:
- **Максимальный размер файла:** 100 MB
- **Рекомендуемый размер репозитория:** < 1 GB
- **Максимальный размер репозитория:** 100 GB (но push может быть очень медленным)

**⚠️ Прямой push 1.7 GB может не сработать или занять очень много времени!**

**Рекомендация: Используй Git LFS (Large File Storage)**

---

## ✅ Команды для добавления

### 1. Добавь аудио файлы

```bash
cd /Users/ilagorelockin/Desktop/NEW/Edinorok

# Добавь все аудио файлы
git add backend/artist_vocals/
git add backend/songs/
git add backend/uploads/

# Или всё сразу
git add backend/
```

### 2. Проверь размер

```bash
# Проверь размер файлов перед коммитом
git diff --cached --stat

# Или проверь размер папок
du -sh backend/artist_vocals backend/songs backend/uploads
```

### 3. Создай коммит

```bash
git commit -m "Добавлены аудио файлы и база данных

- Добавлены аудио файлы артистов (backend/artist_vocals/)
- Добавлены аудио файлы песен (backend/songs/)
- Добавлены загруженные файлы (backend/uploads/)
- Обновлен .gitignore для включения аудио и БД"
```

### 4. Отправь в GitHub

```bash
git push origin main
```

---

## 🚨 Если файлы слишком большие

### Вариант 1: Используй Git LFS (Large File Storage)

```bash
# Установи Git LFS
brew install git-lfs  # macOS
# или
sudo apt install git-lfs  # Linux

# Инициализируй в репозитории
cd /Users/ilagorelockin/Desktop/NEW/Edinorok
git lfs install

# Настрой отслеживание аудио файлов
git lfs track "*.mp3"
git lfs track "*.wav"
git lfs track "*.flac"
git lfs track "*.ogg"
git lfs track "*.m4a"
git lfs track "*.aac"
git lfs track "*.wma"
git lfs track "*.webm"

# Добавь .gitattributes
git add .gitattributes

# Теперь добавь файлы
git add backend/artist_vocals/
git add backend/songs/
```

### Вариант 2: Сожми файлы перед добавлением

```bash
# Создай архив
tar -czf audio_files.tar.gz backend/artist_vocals/ backend/songs/

# Добавь архив
git add audio_files.tar.gz
```

### Вариант 3: Добавь только примеры

```bash
# Добавь только несколько примеров файлов
git add backend/artist_vocals/example1.mp3
git add backend/songs/example1.mp3
```

---

## 📊 Проверка перед push

```bash
# Проверь что файлы добавлены
git status | grep -E "(artist_vocals|songs|uploads)"

# Проверь размер коммита
git diff --cached --stat | tail -1

# Проверь размер отдельных файлов
find backend/artist_vocals backend/songs -type f -exec ls -lh {} \; | awk '{print $5, $9}' | sort -h
```

---

## 💡 Рекомендации

1. **Если файлов много и они большие** - используй Git LFS
2. **Если файлов немного** - можно добавить напрямую
3. **Если файлы очень большие (> 50 MB каждый)** - лучше использовать внешнее хранилище (S3, Google Drive и т.д.)

---

**Готово! Теперь аудио файлы будут в репозитории.** 🎵
