# 📤 Инструкция: Загрузка обновлений в GitHub

## ⚠️ Важно: Что НЕ загружается в GitHub

Следующие файлы и папки **НЕ должны** попадать в репозиторий (слишком большие или содержат данные):

- ❌ `backend/uploads/` - загруженные пользователями аудио файлы
- ❌ `backend/songs/` - аудио файлы песен
- ❌ `backend/artist_vocals/` - аудио файлы артистов
- ❌ `backend/.env` - секретные ключи и пароли
- ❌ `*.db`, `*.sqlite` - файлы баз данных
- ❌ `venv/`, `node_modules/` - зависимости (устанавливаются отдельно)

**Эти файлы исключены через `.gitignore`**

---

## ✅ Что загружается

- ✅ Весь код (Python, JavaScript)
- ✅ Конфигурационные файлы
- ✅ Документация (MD файлы)
- ✅ Скрипты обработки
- ✅ Docker конфигурация
- ✅ Примеры конфигурации (`env.example`)

---

## 🚀 Команды для загрузки

### 1. Проверь статус

```bash
cd /Users/ilagorelockin/Desktop/NEW/Edinorok
git status
```

### 2. Добавь файлы

```bash
# Добавь все новые файлы (кроме игнорируемых)
git add .

# Или выборочно:
git add .gitignore
git add АРХИТЕКТУРА_И_БИБЛИОТЕКИ.md
git add БЫСТРОЕ_ПОДКЛЮЧЕНИЕ_К_БД.md
git add ИНСТРУКЦИЯ_ПОДКЛЮЧЕНИЕ_К_БД.md
git add РЕШЕНИЕ_ПРОБЛЕМЫ_БЛОКОВ.md
git add backend/scripts/check_database.py
git add README.md
git add Dockerfile
git add docker-compose.yml
git add docker-entrypoint.sh
git add .dockerignore
git add DOCKER_README.md
git add backend/env.example
```

### 3. Создай коммит

```bash
git commit -m "Добавлена документация и Docker конфигурация

- Добавлена подробная документация по архитектуре и библиотекам
- Добавлены инструкции по подключению к БД
- Добавлен скрипт check_database.py для проверки БД
- Добавлена Docker конфигурация (Dockerfile, docker-compose.yml)
- Обновлен .gitignore для исключения аудио файлов и БД
- Обновлен README.md"
```

### 4. Отправь в GitHub

```bash
git push origin main
```

---

## 🔍 Проверка перед загрузкой

### Убедись что аудио файлы НЕ добавлены:

```bash
git status | grep -E "(uploads|songs|artist_vocals|\.mp3|\.wav)"
```

Если ничего не выводится - всё правильно! ✅

### Проверь размер коммита:

```bash
git diff --cached --stat
```

Если видишь файлы типа `backend/uploads/` или `*.mp3` - удали их:

```bash
git reset HEAD backend/uploads/
git reset HEAD backend/songs/
```

---

## 📝 Что делать с аудио файлами и БД?

### Аудио файлы

Аудио файлы должны быть на сервере, но **НЕ в git**. 

**Для программиста:**
1. Склонируй репозиторий
2. Помести аудио файлы в нужные папки:
   - `backend/artist_vocals/` - для артистов
   - `backend/songs/` - для песен
3. Запусти скрипты обработки:
   ```bash
   python -m scripts.process_artists
   python -m scripts.process_songs
   ```

### База данных

База данных создается и заполняется на сервере программиста.

**Инструкция для программиста:**
1. Создай базу данных PostgreSQL
2. Настрой `backend/.env` с параметрами подключения
3. Запусти скрипты обработки (см. выше)
4. Проверь через `python -m scripts.check_database`

---

## 🎯 Быстрая команда (всё сразу)

```bash
cd /Users/ilagorelockin/Desktop/NEW/Edinorok

# Проверь что нет аудио файлов
git status | grep -E "(uploads|songs|artist_vocals)" || echo "✅ Аудио файлы не добавлены"

# Добавь все изменения
git add .

# Создай коммит
git commit -m "Обновление: документация, Docker, скрипты проверки БД"

# Отправь в GitHub
git push origin main
```

---

## 🚨 Если возникли проблемы

### "Permission denied"
→ Проверь что у тебя есть права на запись в репозиторий

### "Large files detected"
→ GitHub не позволяет файлы > 100MB. Убедись что `.gitignore` правильно настроен

### "Authentication failed"
→ Обнови токен в `git remote set-url origin https://USER:TOKEN@github.com/...`

---

**Готово! После push все обновления будут на GitHub.** 🎉
